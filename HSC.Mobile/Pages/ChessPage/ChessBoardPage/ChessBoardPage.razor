@page "/"
@using HSC.Mobile.Enums

<Chessboard Size="300" OnMovePlayed="OnMovePlayed" @ref="chessBoard" />

@code {
    private Chessboard chessBoard;

    void OnMovePlayed(MovePlayedInfo mpi)
    {
        //EventService.OnMoveMade(mpi);
        MessagingCenter.Send(this, MessageTypes.MoveMade, mpi);
    }
}
